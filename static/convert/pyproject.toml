[project]
name = "convert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.6.0",
    "safetensors>=0.7.0",
    "tokenizers>=0.22.2",
]
